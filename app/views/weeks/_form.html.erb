<%= form_for(week) do |f| %>
  <% if week.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(week.errors.count, "error") %> prohibited this week from being saved:</h2>

      <ul>
      <% week.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :week_number %>
    <%= f.text_field :week_number %>
  </div>

  <div class="field">
    <%= f.label :week_starts_at %>
    <%= f.datetime_select :week_starts_at %>
  </div>

  <div class="field">
    <%= f.label :week_ends_at %>
    <%= f.datetime_select :week_ends_at %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
